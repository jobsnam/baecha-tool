services:
  - type: web
    name: baecha-tool
    runtime: python
    buildCommand: sudo apt-get install -y tesseract-ocr tesseract-ocr-kor && pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
